$('#ajax-loading').css("visibility","hidden");
$('#ajax-loading').before("<%= j (render 'shared/notifications', :local_activities => @dropdown_activities) %>");
//$('#<%=notif_id%>').hide();
//$('#<%=notif_id%>').fadeIn(500);

<% if @dropdown_activities.current_page >= @dropdown_activities.total_pages %>
	can_request_notif = false;
	$('#notif-loading').css("display","none");
<% else %>
	can_request_notif = true;
<% end %>