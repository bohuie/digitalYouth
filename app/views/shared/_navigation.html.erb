<nav class="navbar navbar-default">
  <div class="container container-fluid">
    <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
        <a class="navbar-brand" href="/">Home Icon</a>
    </div>

    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <% if user_signed_in? %> <!-- Added the active part for style. Can be removed -->
          <li class="<%= 'active' if current_page?(user_path(current_user)) %>"><%= link_to('My Profile', current_user) %></li>
        <% end %>
      </ul>

      <form class="navbar-form navbar-left" role="search">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search">
        </div>
        <button type="submit" class="btn btn-default"><span class="glyphicon glyphicon-search"></span></button>
      </form>

      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= link_to(current_user.first_name, current_user, class: "navbar-brand", style: "cursor:default;") %></li>

          <li class="dropdown">                                   
            <%= link_to show_notifications_path, html_options = 
            {class: "dropdown-toggle", "data-toggle"=>"dropdown","data-remote"=>"true"}, remote: true do %>
              <span class="glyphicon glyphicon-bell notifications-icon" aria-label="Notifications"></span>
              <span id="notif-num"><%= @notif_unseen %></span>
            <% end %>

            <ul class="dropdown-menu notif-bar" role="menu">
                <li id="notif-list">
                  <div class="progress"> 
                    <div class="progress-bar progress-bar-striped active" role="progressbar" aria-valuenow="100" aria-valuemax="100" style="width: 100%"></div>
                  </div>
                </li>

              <li><%= link_to("Show all notifications", notifications_path, class: "center-text") %></li> 
            </ul>
          </li>

          <li><%= link_to('Logout', destroy_user_session_path, :method => :delete) %></li>
        <% else %>
          <li><%= link_to('Login', new_user_session_path)  %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>