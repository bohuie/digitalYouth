
<%= simple_form_for (@user) do |u| %>
	<%= u.input :first_name %>
	<%= u.input :last_name %>
	<%= u.input :email %>
	<%= u.input :password %>
	<%= u.input :confirm_password %>
	<%= u.input :github %>
	<%= u.input :linkedin %>
	<%= u.input :twitter %>

	<%= u.submit %>
<% end %>