<%j = activity.trackable %>
<div>
	<%= image_tag j.company.image.url(:thumb), alt:"User Icon", class:"image-radius thumb-shadow", style: "margin-right: 5px;" %>
</div>
<div class="text">
	<%=j.company.company_name%>
	<% if p[:status] == -1 %>
		has declined your application.
	<% elsif p[:status] == 1 %>
		is considering your application.
	<% elsif p[:status] == 2 %>
		has accepted your application.
	<% end %>
	<br>
	<%= local_time_ago(activity.created_at) %>
</div>
<div style="clear:both;"></div>